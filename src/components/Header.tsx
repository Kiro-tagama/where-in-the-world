export default function Header(props:any) {
  return(
    <header>
      <h1 className=" font-bold text-lg">Where in the world?</h1>
      <button>
        Dark mode
      </button>
    </header>
  )
}